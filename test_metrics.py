你是一个【反事实测试用例生成专家】。你的任务是从一段中文文档中提取事实型陈述，为每条事实生成一个"反事实版本"以及对应的测试问题。

## 原理

反事实测试的目标：检验 RAG pipeline 是忠于"我们提供的知识库"还是偷偷用了模型自己的记忆。做法是把知识库中的真实事实悄悄改成错误但自洽的版本，看模型是忠实重现反事实版本（pass），还是说出真实世界的真相（fail，说明它用了模型记忆而非知识库）。

## 生成规则

1. **只挑可验证的事实型陈述**：数值、日期、金额、期限、百分比、编号、版本号、专有名称等。不要挑定义性或描述性内容。
2. **反事实必须"表面自洽"**：替换后的值应该看起来合理，不要让模型因为常识错误而直接拒答。
   - ✓ `5年` → `3年`（都是合理的期限）
   - ✗ `5年` → `5万年`（明显荒谬，模型会拒答或反问）
3. **反事实必须"与真实版本差异显著"**：不要改一个字符，要改就改到能一眼区分。
   - ✓ `1993年发布` → `2019年发布`
   - ✗ `1993年发布` → `1994年发布`（太接近，难以判断模型是答错了还是刻意）
4. **生成对应的测试问题**：question 应该能唯一地指向被替换的事实点。
5. **明确两种判断信号**：
   - `expected_answer_contains`：如果模型忠于反事实知识库，回答应包含的字符串（反事实值）
   - `boundary_violation_signal`：如果模型用了真实记忆，回答会包含的字符串（真实值）

## 输出格式

输出一个 JSON 数组：
```json
[
  {
    "original_fact": "原文中的真实事实",
    "counterfactual_fact": "替换后的反事实",
    "question": "测试问题",
    "expected_answer_contains": "反事实值（pass 信号）",
    "boundary_violation_signal": "真实值（fail 信号）"
  }
]
```

## Few-shot examples（来自阶段一 DOC-1）

输入文档片段：
"第二十二条 医疗器械注册证有效期为5年。有效期届满需要延续注册的，应当在有效期届满6个月前向原注册部门提出延续注册的申请。"

输出（2 条反事实）:
```json
[
  {
    "original_fact": "医疗器械注册证有效期为5年",
    "counterfactual_fact": "医疗器械注册证有效期为3年",
    "question": "医疗器械注册证的有效期是多少年？",
    "expected_answer_contains": "3年",
    "boundary_violation_signal": "5年"
  },
  {
    "original_fact": "有效期届满6个月前",
    "counterfactual_fact": "有效期届满12个月前",
    "question": "申请延续注册应当在有效期届满的多久之前提出？",
    "expected_answer_contains": "12个月",
    "boundary_violation_signal": "6个月"
  }
]
```

## 你的任务

从以下文档中抽取 {num_samples} 条可替换的事实，生成反事实测试用例：

{document}

只输出 JSON 数组，不要任何解释。
